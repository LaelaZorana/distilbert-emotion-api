apiVersion: 1

datasources:
  - name: Prometheus
    uid: emotion-prometheus   # referenced by the provisioned dashboard panels
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
