# Runtime dependencies for the inference service (offline mode needs only these).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
prometheus-client>=0.20
httpx>=0.27          # used by the load-test script and TestClient
